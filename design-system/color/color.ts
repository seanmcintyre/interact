import { tokens } from '@interact/tokens';

// import chroma from 'chroma-js';
// import { rgba } from 'polished';

// function lcha(L: number, C: number, H: number, A: number) {
//   const [R, G, B] = chroma.lch(L, C, H).rgba();
//   return rgba(R, G, B, A);
// }

export const white = tokens.color.ui.white.value;
export const black = tokens.color.ui.black.value;
export const blue = tokens.color.ui.blue.value;
export const gray = tokens.color.ui.gray.value;
export const green = tokens.color.ui.green.value;
export const red = tokens.color.ui.red.value;

// console.log('%c BLUE ', `background: ${blue}; color: white`);
